/**************************************************************************************
 * @file         : demo20.c
 * @brief        : 找出由数字 1、2、3、4 组成的所有互不相同且无重复数字的三位数，并输出。
 *                 这段代码遍历从123到432的所有三位数，检查其中是否包含数字1、2、3、4且各位数字互不相同，
 *                 符合条件的则输出。
 *                 注意：题目要求的三位数是指百位、十位、个位上都不相同的数字。
 * @author       : a_23456@foxmail.com
 * @date         : 2024-07-16 10:17:20
 * @version      : 1.0
 * @note         :
 *                 这段代码通过遍历 123 到 432 的所有三位数，使用数学运算和条件判断来筛选出符合条件的数字。
 *                 确保每个三位数的百位、十位、个位均为数字 1、2、3、4，且各位数字互不相同。
 *                 输出符合条件的三位数。
 * @Copyright    :
 *                 (c)   2024-2025   a_23456@foxmail.com
 *                 All Right Reserved
 **************************************************************************************/

#include <stdio.h>

int main(int argc, const char *argv[]) {
    // 遍历从123到432的所有三位数
    for (int num = 123; num <= 432; num++) {
        // 分别获取当前三位数的个位、十位、百位数字
        int units = num % 10;      // 个位
        int tens = num / 10 % 10;  // 十位
        int hundreds = num / 100;  // 百位

        // 检查是否满足题目要求：各位数字互不相同且均为1、2、3、4
        if (units != tens && units != hundreds && tens != hundreds &&
            (units == 1 || units == 2 || units == 3 || units == 4) &&
            (tens == 1 || tens == 2 || tens == 3 || tens == 4) &&
            (hundreds == 1 || hundreds == 2 || hundreds == 3 || hundreds == 4)) {
            printf("%d ", num); // 输出符合条件的三位数
        }
    }
    printf("\n");

    return 0;
}
