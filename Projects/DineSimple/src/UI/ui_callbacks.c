/****************************************************************************************************************************************************************
 * @file         : /Code/Projects/DineSimple/src/UI/ui_callbacks.c
 * @brief        : 回调函数实现
 * @author       : a_23456@foxmail.com
 * @date         : 2024-08-16 21:09:13
 * @version      : 1.0
 * @note         : 实现了与 UI 相关的回调函数
 * @Copyright    : (c)   2024-2025   a_23456@foxmail.com   All Right Reserved
 *****************************************************************************************************************************************************************/
#include "../../include/UI/ui_callbacks.h"
#include <stdio.h>

typedef struct {
  int x;
  int y;
} TouchEventData;

/****************************************************************************************************************************************************************
 * @name: welcome_start_order_callback
 * @brief: 处理开始点餐按钮的回调函数
 * @param {void*} data - 触摸事件数据
 * @return: void
 * @note: 该函数在点击开始点餐按钮时被调用
 *****************************************************************************************************************************************************************/
void welcome_start_order_callback(void* data) {
  TouchEventData* eventData = (TouchEventData*)data;
  printf("开始点餐按钮被点击: (%d, %d)\n", eventData->x, eventData->y);
  // 这里可以添加处理开始点餐逻辑的代码
}

/****************************************************************************************************************************************************************
 * @name: welcome_user_login_callback
 * @brief: 处理用户登录按钮的回调函数
 * @param {void*} data - 触摸事件数据
 * @return: void
 * @note: 该函数在点击用户登录按钮时被调用
 *****************************************************************************************************************************************************************/
void welcome_user_login_callback(void* data) {
  TouchEventData* eventData = (TouchEventData*)data;
  printf("用户登录按钮被点击: (%d, %d)\n", eventData->x, eventData->y);
  // 这里可以添加处理用户登录逻辑的代码
}

/****************************************************************************************************************************************************************
 * @name: welcome_shop_login_callback
 * @brief: 处理商家登录按钮的回调函数
 * @param {void*} data - 触摸事件数据
 * @return: void
 * @note: 该函数在点击商家登录按钮时被调用
 *****************************************************************************************************************************************************************/
void welcome_shop_login_callback(void* data) {
  TouchEventData* eventData = (TouchEventData*)data;
  printf("商家登录按钮被点击: (%d, %d)\n", eventData->x, eventData->y);
  // 这里可以添加处理商家登录逻辑的代码
}
